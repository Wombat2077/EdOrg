<Window x:Class="EdOrg.Views.StudentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EdOrg.Views"
        mc:Ignorable="d"
        Title="Stident" Height="450" Width="800">
    <StackPanel VerticalAlignment="Center"> 
        <ComboBox x:Name="cbxCurrentSubject" SelectionChanged="ChangeCurrentSubject" HorizontalAlignment="Center" VerticalAlignment="Center" IsEditable="True" IsReadOnly="True" Text="выберите дисциплину"></ComboBox>
        <DataGrid VerticalAlignment="Bottom" Margin="10" x:Name="dgMarks" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата"  Width="*" Binding="{Binding CreatedAt}" />
                <DataGridTextColumn Header="Дисциплина"  Width="*" Binding="{Binding Subjects.Name}" />
                <DataGridTextColumn Header="Оценка" Width="*" Binding="{Binding Value}" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</Window>
