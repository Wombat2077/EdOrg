<Window x:Class="EdOrg.Views.StudentView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EdOrg.Views"
        mc:Ignorable="d"
        Title="Student" Height="450" Width="800">
    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="350">
        <Image Source="../Resources/sad-pepe.png" Height="100" HorizontalAlignment="Center"/>
        <Label HorizontalAlignment="Left" Margin="10,0,0,0">Дисциплина</Label>
        <ComboBox x:Name="cbxCurrentSubject" Margin="10,0,0,0" SelectionChanged="ChangeCurrentSubject" HorizontalAlignment="Left" VerticalAlignment="Center" Width="100"></ComboBox>
        <DataGrid VerticalAlignment="Bottom" IsReadOnly="True" CanUserAddRows="False"  Margin="10" x:Name="dgMarks" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата"  Width="*" Binding="{Binding CreatedAt}" />
                <DataGridTextColumn Header="Дисциплина"  Width="*" Binding="{Binding Subjects.Name}" />
                <DataGridTextColumn Header="Оценка" Width="*" Binding="{Binding Value}" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
</Window>
